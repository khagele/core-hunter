# deps / build
node_modules/
dist/
.vite/

# runtime config (broker creds etc.) — never commit
config.json
public/config.json
.env
.env.*

# logs / os
*.log
.DS_Store
Thumbs.db

# local agent context (kept locally, not published)
CLAUDE.md
